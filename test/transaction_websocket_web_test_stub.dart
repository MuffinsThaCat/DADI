// This is a stub file for non-web platforms to avoid dart:html errors in tests
// The actual tests are in transaction_websocket_web_test.dart and will only run on web platforms

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('TransactionWebSocketServiceWeb Tests (Stub for non-web)', () {
    test('Skipping web-specific tests on non-web platform', () {
      // This is just a placeholder test that always passes
      expect(true, isTrue);
    });
  });
}
